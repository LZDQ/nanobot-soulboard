# Domain and path prefix for backend API/WebSocket calls.
# "" -> /api/..., "/prefix" -> /prefix/api/..., "http://example.com/aaa" -> http://example.com/aaa/api/...
# Leave empty (or unset) when the backend serves the built frontend at the origin root.
VITE_API_BASE=http://127.0.0.1:18791
